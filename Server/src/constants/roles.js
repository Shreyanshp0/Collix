export const GROUP_ROLES = Object.freeze({
	OWNER: 'owner',
	ADMIN: 'admin',
	MODERATOR: 'moderator',
	MEMBER: 'member',
});

export const GROUP_ROLE_VALUES = Object.freeze(Object.values(GROUP_ROLES));
